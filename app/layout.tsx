import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "outshare",
  description: "Create a highly personalized profile page designed for outdoor enthusiasts, serving as an 'outdoor version of Linktree.' This page should feature a clean, modern, and appealing interface, centralizing the display of their travel logs, personal gear inventory, and social media links.",
  icons: {
    icon: "/outshare.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
